# CHANGELOG

本文件记录 Protocol 各版本的功能变更、已知限制与后续方向。

---

## v0.3.0（当前版本）

**发布质量基准线**

### CTDP 已完成能力

- 主链创建与管理（名称、神圣座位触发动作、专注时长、完成条件）
- 正式任务启动 / 完成（完成延长链长度）
- 正式任务失败裁决：违规清零 或 判例化
- 辅助链配置（内嵌于每条主链，支持触发动作、延迟、完成条件）
- 辅助链启动 / 确认窗口 / 履约进入正式任务
- 辅助链到期待裁决（非自动失败）
- 辅助链裁决：违约清零（仅辅助链清零，不影响主链长度）或辅助链判例化
- 辅助链独立当前长度和最佳长度
- 判例库管理：查看详情、编辑标题/描述、废止（保留历史但不作为活跃边界）
- 协议边界展示（主链判例与辅助链判例合并，标签区分来源）
- 轻量失败调试记录（触发动作太重、完成条件过高、时间太长、环境不适合、规则不清、状态不足、其他）
- Dashboard 协议状态指示（无活动 / 进行中 / 待裁决 / 预约倒计时）
- History 事件时间线（CTDP + 辅助链 + 判例事件）
- 预约判定守卫（确认窗口未结束不可裁决）

### RSIP 已完成能力

- 定式节点创建（根定式 / 子定式）
- 定式树缩进展示（COALESCE 排序）
- 定式点亮（激活）/ 熄灭（停用）
- 父节点熄灭时递归回滚活跃子节点
- formula_events 事件日志（created / activated / deactivated / rollback_child_deactivated）
- Dashboard RSIP 摘要（总数 / 活跃数 / 非活跃数 / 最新事件）
- History 统一协议时间线（CTDP + 辅助链 + RSIP 事件）
- 单定式复盘面板：生命周期、熄灭备注、回滚影响、事件历史
- 定式标题和执行说明编辑
- 熄灭时用户自定义备注（替代默认描述）

### 基础架构

- Tauri 2 桌面外壳
- React 19 + TypeScript 前端
- Rust 后端（rusqlite bundled SQLite）
- 本地 SQLite 存储（WAL 模式，外键约束）
- 离线优先，无云端依赖
- 数据库迁移框架（prefix 列、前置 schema 兼容）
- TypeScript 类型检查 + ESLint 代码质量
- 已有 Rust 单元测试（预约阶段、判例守卫、RSIP 清洁函数等）

### 当前已知限制

- 不包含桌面通知、窗口置顶或系统锁定
- 不包含 RSIP 国策组容错
- 不包含水密隔舱
- 不包含 RSIP 定式向导
- 不包含数据导出 / 导入
- 不包含多语言支持（仅中文界面）
- 不包含自动化 CI/CD 流水线
- 安装包签名待配置

### 后续方向

- 桌面通知与提醒
- 数据备份恢复（导出导入 protocol.db）
- RSIP 国策组容错
- RSIP 周期复盘
- 安装包签名
- Windows / macOS / Linux 三平台正式构建验证

---

## v0.2.1（上一版本）

### 主要变化

- 主链失败后进入轻量裁决
- 辅助链具有独立当前长度和最佳长度
- 辅助链到期后先进入确认窗口，确认窗口结束后进入待裁决
- 辅助链裁决支持违约清零或判例化
- 判例库支持详情查看、编辑和废止
- RSIP 单定式复盘支持标题/执行说明维护
- 熄灭前填写自定义备注
- History 页使用协议语言展示 CTDP、辅助链和 RSIP 事件
- 当前产品边界是完整离线桌面版

---

## v0.2.0（V2 Alpha）

### 主要变化

- RSIP 定式树接入 Dashboard 和 History
- 定式节点、父子结构、点亮/熄灭、递归回滚、事件日志
- CTDP 主链和预约链基础功能
- 最小可用离线桌面版本

---

## v0.1.0（CTDP V1）

### 初始版本

- 主链创建与管理
- 正式任务启动/完成
- 预约启动机制
- 基础判例库
- Dashboard 和 History 基础展示
