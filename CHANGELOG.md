# CHANGELOG

本文件记录 Protocol 各版本的功能变更、已知限制与后续方向。

> 标记说明：
> - **M** = 已在 master 分支，当前可运行
> - **R** = 本分支（feature/release-quality）新增的发布质量保障
> - **P** = 后续计划，未实现

---

## v0.3.0（待合并发布）

**状态：** 本分支为发布准备分支。v0.3.0 正式版本应在所有目标分支合并到 master 后，从 master 构建。

### 本分支做了什么

| 事项 | 归属 |
|------|------|
| 统一版本号到 0.3.0 | **R** |
| README 重写（产品定位、功能范围、排除项、故障排查） | **R** |
| CHANGELOG.md（本文件） | **R** |
| docs/QA_CHECKLIST.md（冒烟测试清单） | **R** |
| docs/RELEASE_PROCESS.md（发布检查、打包、回滚流程） | **R** |
| npm run typecheck / check / check:full | **R** |

### CTDP 已完成能力

以下能力已在 master 分支完整可用：

- 主链创建与管理（名称、神圣座位触发动作、专注时长、完成条件）**M**
- 正式任务启动 / 完成（完成延长链长度）**M**
- 正式任务失败裁决：违规清零 或 判例化 **M**
- 辅助链配置（内嵌于每条主链，支持触发动作、延迟、完成条件）**M**
- 辅助链启动 / 确认窗口 / 履约进入正式任务 **M**
- 辅助链到期待裁决（非自动失败）**M**
- 辅助链裁决：违约清零（仅辅助链清零，不影响主链长度）或辅助链判例化 **M**
- 辅助链独立当前长度和最佳长度 **M**
- 判例库管理：查看详情、编辑标题/描述、废止（保留历史但不作为活跃边界）**M**
- 协议边界展示（主链判例与辅助链判例合并，标签区分来源）**M**
- 轻量失败调试记录（触发动作太重、完成条件过高、时间太长、环境不适合、规则不清、状态不足、其他）**M**
- Dashboard 协议状态指示（无活动 / 进行中 / 待裁决 / 预约倒计时）**M**
- History 事件时间线（CTDP + 辅助链 + 判例事件）**M**
- 预约判定守卫（确认窗口未结束不可裁决）**M**

### RSIP 已完成能力

以下能力已在 master 分支完整可用：

- 定式节点创建（根定式 / 子定式）**M**
- 定式树缩进展示（COALESCE 排序）**M**
- 定式点亮（激活）/ 熄灭（停用）**M**
- 父节点熄灭时递归回滚活跃子节点 **M**
- formula_events 事件日志（created / activated / deactivated / rollback_child_deactivated）**M**
- Dashboard RSIP 摘要（总数 / 活跃数 / 非活跃数 / 最新事件）**M**
- History 统一协议时间线（CTDP + 辅助链 + RSIP 事件）**M**
- 单定式复盘面板：生命周期、熄灭备注、回滚影响、事件历史 **M**
- 定式标题和执行说明编辑 **M**
- 熄灭时用户自定义备注（替代默认描述）**M**

### 基础架构

- Tauri 2 桌面外壳 **M**
- React 19 + TypeScript 前端 **M**
- Rust 后端（rusqlite bundled SQLite）**M**
- 本地 SQLite 存储（WAL 模式，外键约束）**M**
- 离线优先，无云端依赖 **M**
- 数据库迁移框架（prefix 列、前置 schema 兼容）**M**
- TypeScript 类型检查 + ESLint 代码质量 **M**（`npm run typecheck` / `check` / `check:full` 脚本由 **R** 新增）
- Rust 单元测试 16 个（预约阶段、判例守卫、RSIP 清洁函数等）**M**

### 当前已知限制

- 不包含桌面通知、窗口置顶或系统锁定
- 不包含数据管理页面（备份/恢复/导出），已在 feature/data-management 分支开发，待合并 **P**
- 不包含 RSIP 国策组容错 **P**
- 不包含水密隔舱 **P**
- 不包含 RSIP 定式向导 **P**
- 不包含多语言支持（仅中文界面）
- 不包含自动化 CI/CD 流水线
- 安装包签名待配置 **P**

### 后续方向

- 合并 feature/data-management（数据备份/恢复/导出/清理）
- 桌面通知与提醒
- RSIP 国策组容错
- RSIP 周期复盘
- 安装包签名
- Windows / macOS / Linux 三平台正式构建验证

---

## v0.2.1（master 当前版本）

**此版本已在 master 分支。**

### 主要变化

- V2 Gamma 完成：辅助链独立长度、第二预约信号、RSIP 单定式复盘
- 辅助链裁决闭环（确认窗口到期 → 待裁决 → 违约清零或判例化）
- 判例库成熟化第一版（查看详情、编辑、废止，保留历史）
- RSIP 单定式维护第一版（标题/说明编辑、自定义熄灭备注）
- History 页使用协议语言展示 CTDP、辅助链和 RSIP 事件
- 当前产品边界是完整离线桌面版

---

## v0.2.0（V2 Alpha）

**此版本已在 master 分支。**

### 主要变化

- RSIP 定式树接入 Dashboard 和 History
- 定式节点、父子结构、点亮/熄灭、递归回滚、事件日志
- CTDP 主链和预约链基础功能
- 最小可用离线桌面版本

---

## v0.1.0（CTDP V1）

**此版本已在 master 分支。**

### 初始版本

- 主链创建与管理
- 正式任务启动/完成
- 预约启动机制
- 基础判例库
- Dashboard 和 History 基础展示
